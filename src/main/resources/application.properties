#spring.application.name=spring-vault
spring.cloud.vault.token=${VAULT_ADMIN_TOKEN}
spring.cloud.vault.uri=${VAULT_ADDR}
spring.cloud.vault.scheme=http
spring.cloud.vault.authentication=token
spring.cloud.vault.kv.enabled=true
spring.cloud.vault.kv.application-name=spring-vault,global
spring.config.import: vault://
server.port=${port}
#server.port=${port-number}
#spring.profiles.active=spring-vault, global
